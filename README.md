# kernel-dev-skill

This folder is a Claude skill, not a user-facing project README.

Read `SKILL.md` for the workflow.
Read `references/` only when the active task needs more detail.
Primary materials are now under `references/labs/` and `references/lectures/`.
